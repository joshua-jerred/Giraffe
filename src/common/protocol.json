{
  "required_fields": {
    "src":{
      "type":"enum",
      "options":["gfs", "ggs", "telemetry", "client"]
    },
    "dst":{
      "type":"enum",
      "options":["gfs", "ggs", "telemetry", "client"]
    },
    "typ":{
      "type":"enum",
      "options":["info", "req", "rsp"]
    },
    "cat":{
      "type":"enum",
      "options":["ping", "data", "error", "stream", "path"]
    },
    "id":{
      "type":"string"
    },
    "body":{
      "type":"object"
    }
  }
}